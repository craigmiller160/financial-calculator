export {};

describe('calculateTaxesForBonuses', () => {
	it('calculates for past bonuses', () => {
		throw new Error();
	});

	it('calculates for future bonuses', () => {
		throw new Error();
	});
});
