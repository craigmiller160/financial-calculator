export {};

describe('calculateTaxesForPaychecks', () => {
	it('calculates for past paychecks', () => {
		throw new Error();
	});

	it('calculates for future paychecks', () => {
		throw new Error();
	});
});
