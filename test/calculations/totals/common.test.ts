export {};

describe('totals common', () => {
	it('getRate401k', () => {
		throw new Error();
	});

	it('getAmount401k', () => {
		throw new Error();
	});

	it('getPayrollTaxCosts', () => {
		throw new Error();
	});
});
