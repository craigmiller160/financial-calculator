import { getTestData } from '../testutils/getTestData';

describe('validateData', () => {
	it('data is valid', () => {
		throw new Error();
	});

	it('data has duplicate name', () => {
		throw new Error();
	});
});
