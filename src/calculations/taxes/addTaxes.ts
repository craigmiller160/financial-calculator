import { DataWithTotals } from '../totals/TotalTypes';
import { TryT } from '@craigmiller160/ts-functions/types';

export const addTaxes = (data: DataWithTotals): TryT<DataWithTotals> => {
	throw new Error();
};
